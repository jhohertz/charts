apiVersion: v1
kind: Service
metadata:
  name: {{ template "spinnaker.fullname" . }}-deck
  labels:
    app: {{ template "spinnaker.fullname" . }}
    component: deck
spec:
  type: {{ .Values.serviceType }}
  ports:
    - port: 9000
  selector:
    app: {{ template "spinnaker.fullname" . }}
    component: deck
